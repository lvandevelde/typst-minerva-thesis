#import "@local/minerva-thesis:0.3.0": *

#import "../defs.typ": *


= Basisprincipes van het onderwerp<ch:funda>

In dit hoofdstuk bouwen we verder op  @ch:intro en op @sbsctn:introsubsection in het bijzonder.



== Historische achtergrond <sctn:fundahist>


#m-figure(image("img/figure_2.svg"), caption: [De eerste figuur van het tweede hoofdstuk], placement: bottom , label: <fig:funda-1>)

$

A &= a sin alpha

$ <eq:asinalpha>

Voorbeelden van figuren van het door de gebruiker gedefinieerde type ``` theorem```:

#m-figure([$a+b=c<=>a=c-b$], kind: "theorem", caption: [Een figuur van type ``` theorem``` ], label: <theo:add>)
#m-figure([$a+b=c<=>b=c-a$], kind: "theorem", caption: [Een andere figuur van type ``` theorem```], label: <theo:add2>)

In @theo:add, ...

#ref-list(<theo:add>,<theo:add2>)


=== Een subparagraaf


@fig:appA 

== Een paragraaf met een zeer lange titel om de layout te controlleren van een inhoudstafel met titels over meerdere lijnen



